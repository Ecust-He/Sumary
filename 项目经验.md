# 项目一：云原生平台和应用部署工具开发
## 项目介绍：自动化集成增强一键式部署工具，负责云原生应用和其依赖的云资源的生命周期管理。
## 技术栈：Golang、k8s、CRD + Operator二次开发、corbra命令行框架、Helm Chart部署、Go Template模板语言等
## 项目经验：负责安装部署任务调度框架、任务步骤编排框架、任务流程编排产品二次开发扩展点框架、支持产品自定义版本包结构功能、应用部署Operator二次开发、部署技术服务operator、部署应用等多个核心模块方案的设计和实现

### 任务调度框架
#### 关键设计点
1、**定时轮询**：调度器每隔1s定时查询待执行的任务
2、**异步执行**：每个任务在单独的goroutine中执行
3、**状态驱动**：通过状态机控制task、stage、step的执行流程
4、**用户干预**：用户在特定的节点对任务进行人工干预，比如step失败后用户可以进行重试、忽略跳过、终止等操作

#### 调度器核心流程
Step 1：程序启动时，开一个goroutine每隔1s 根据任务的状态定时查询待执行的任务，依次遍历task中的stages、每个stage中的steps，刚开始执行task、stage、step时状态置为执行中，调度器设计上对Step的执行（StepHandler）做了层抽象，通过接口 + 注册表 + 工厂模式实现，每个step都需要实现DO和Check方法，依次调用DO（调用命令行）和Check（捕捉标准输出流的关键字）方法
Step 2: 如果执行成功，则继续执行下一个step；
        如果执行失败，则需要根据运行模式（失败后停止和失败后继续），如果是执行失败后继续，则只更新step的状态为执行失败，任务继续；
                                                               如果是执行后停止，则依次更新task、stage、step的状态为执行失败，该任务从待执行的任务队列中移除
#### 用户操作干预任务调度流程
step执行失败后，用户可以对该step进行重试、忽略跳过、终止操作
Step 1：用户点击“重试”按钮，需要将任务加入待执行任务队列中，依次更新task、stage、step为待执行状态，调度器在下一次轮询时会执行该步骤
Step 2:用户点击“忽略跳过”按钮，需要将该任务加入待执行任务队列中，依次更新task、stage为待执行状态、step为finished状态，调度器在下一次轮询时会执行下一个步骤
Step 2:用户点击“终止”按钮，需要将该任务从待执行任务队列中移除，依次更新task、stage、step为终止状态

特殊场景：
1、运行模式为用户阻塞模式的step
   1）任务调度器执行到该step时，该任务会立即停止执行，并从待执行的任务队列中移除
   2）用户点击“继续”按钮时，会将运行模式从“用户阻塞状态”切换到“执行失败后停止”，，依次更新task、stage、step的状态为待执行状态，将该任务调度到待执行的任务队列中

2、每个stage执行完成后，需要用户确认后执行下一个stage
   1）任务调度器在task的每个stage执行完后，将该stage状态置为waitFinished，并终止该任务执行
   2）用户点击“继续”按钮时，将该stage状态置为finished，task状态置为待执行，调度器在下一次轮询时，将该任务从待执行的任务队列调度

备注：
1、任务的状态都为终态（执行成功、终止），才允许用户创建新的任务

### 任务步骤编排框架
根据任务类型和选择的模块范围加载不同的工具包，通过解析workflow.yaml文件动态编排任务。workflow.yaml描述了该任务中有哪些stage和step
#### 关键设计点
1、**声明式编排**：通过workflow.yaml文件
2、**步骤执行统一处理器**： 执行step并检查是否执行成功

### 任务流程编排二次开发扩展点框架
对产品开放二次开发能力，扩展了workflow.yaml文件任务编排能力，二次开发扩展点meta.conf描述文件主要定义执行时机和执行入口
扩展点功能主要有：
1、多语言支持：支持shell、python、二进制文件
2、支持配置timeout超时时间
2、支持扩展点运行在堡垒机和aio-lcm容器内

#### 关键设计点
1、**扩展点插入机制**：根据执行时机，动态加载到workflow.yaml文件中的某个stage的step前面/后面
2、**扩展点执行机制**：定义扩展点执行器用于执行扩展点，支持多种类型，比如运行在堡垒机上扩展点，需要ssh拷贝到堡垒机上运行等
3、**扩展点传参**：通过环境变量方式，解耦设计

高级功能：
1、给扩展点传递文件类型的参数
2、扩展点支持导出文件

### 支持产品自定义版本包结构功能
产品通过配置manifest.json描述文件，将产品自定义版本包结构转换成标准包结构
manifest.json文件中主要包括文件类型（file、dir、slice）、权重（定义文件处理顺序，权重相同并发执行）、版本类型（平台版本包、应用版本包、扩展点）、重命名、目标目录、checksum校验（校验文件是否损坏）等，方案设计时还考虑了源文件不存在是否报错、目标文件存在是否覆盖等异常场景


### 应用部署Operator二次开发
kubebuilder脚手架工具生成
1、部署和卸载应用cr调谐逻辑：
   1）部署应用：监听到cr的创建，如果cr的操作状态是终态（执行成功或执行失败），跳过处理
                              如果cr的操作状态是执行中(Creating），隔5s查询部署任务的状态（待执行、执行中、执行成功、执行失败），并根据任务状态更新cr中操作状态
                              如果cr的操作状态是初始状态，调用应用生命周期管理Pod的卸载应用异步接口，根据返回的任务ID,更新cr的操作状态为执行中(Creating）
   2）卸载应用：监听到cr的删除， 如果cr的操作状态是删除中（Deleting），隔5s查询卸载任务的状态（待执行、执行中、执行成功、执行失败），并根据任务状态更新cr的操作状态
                              如果cr的操作状态是终态（执行成功或执行失败），调用应用生命周期管理Pod的卸载应用异步接口，根据返回的任务ID，更新cr的操作状态为删除中（Deleting）
2、应用升级/扩容等操作cr调谐逻辑：
   1）监听到cr的删除，跳过处理
   2）如果cr的操作状态是终态（任务状态为执行跳过或执行失败），跳过执行
   3）如果cr的操作状态为Creating，隔5s查询卸载任务的状态（待执行、执行中、执行成功、执行失败），并根据任务状态更新cr中操作状态
 
备注：
1、删除cr时，一定要要将Finalizer从CR中删除，否则CR一直处于Deleting状态，不会真正被k8s删除
2、在部署应用时，为了解决并发冲突问题，使用cas原理通过metadata中resourceVersion字段进行乐观锁，如果有变化则说明已有部署应用任务

### 部署技术服务Operator
1、使用go template渲染后的技术服务的rbac对应的values.yaml，通过helm chart方式，部署技术服务的rbac
2、使用go template渲染后的技术服务的operator对应的CR，通过CR方式，部署技术服务的operator

### 部署应用
从软仓下载应用版本包并解压，然后根据用户选择的场景文件（描述需要部署哪些app）和资源文件（描述app中pod的cpu、memory等），调用应用安装工具命令行进行部署
1、镜像构建使用统一的install.sh脚本（构建镜像、上传chart包）
2、使用helm chart方式部署应用

# 项目二：非云原生应用部署工具开发
## 项目介绍：一键式应用部署工具，负责非云原生应用和其依赖的非云资源的生命周期管理。
## 技术栈：Golang、CSP并发模型、corbra命令行框架等
## 项目经验：负责公共服务实体实例创建、应用pvc创建、镜像构建、证书上传、全量和补丁版本包合并等多个核心模块方案的设计和实现

### CSP模型
并发哲学：不要通过共享内存来通信，而应通过通信来共享内存。
CSP模型：一个符合CSP模型的应用程序，应该通过一组输入/输出原语连接起来P的集合

并发原语
1、goroutine:对应CSP模型中的P，封装了数据处理逻辑，是go运行时调度的基本执行单元
2、channel：对应CSP模型中的输入/输出原语，用于goroutine之间的通信和同步
3、select：应对多路输入输出，协调多个goroutine进行操作

业务流程：
1、app压缩包解压、镜像构建、创建公共服务实例、创建pvc、部署app
2、
3、平台接口限流：创建pvc接口需要控制并发数，使用带缓冲区的channel，用作计数信号量
4、将app的部署结果写到一个结果文件中，包括app的名称，部署成功结果、失败的步骤和原因等


备注：
1、公共服务的实体/实例联系和区别，比如：pg数据库实体->pg server（pg数据库pod），pg数据库一个实例（指定用户名和密码）->pg数据库的一个db_name
2、部署app的csp模型channel传递的数据包括app名称、阶段（比如构建镜像）、错误信息error等




遗留问题：
1、网管自动化运维平台
2、k8s的调度架构

# 项目二：网元数据流式清洗框架开发
## 项目介绍：对网元上报的数据进行时间粒度和空间粒度的汇总，汇总完之后数据入湖。
## 技术栈：Java、C++、kafka等
## 项目经验：负责算法生成模块、算法提交模块、数据清洗任务参数校验框架等多个核心模块方案的设计和实现

### 数据清洗任务参数校验框架
使用责任链模式 + 建造者模式
1）定义一个抽象Irule接口，包含validate方法和setNext方法，
   validate方法中编写业务逻辑，如果验证通过，则调用nextValidate方法
   setNext方法用于构造RuleChainBuiler类
2）定义一个RuleChainBuiler类，构建责任链，内部数据结构使用链表

