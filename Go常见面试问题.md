
# goroutine调度原理
## goroutine调度器
将goroutine按照一定算法调度到CPU执行的程序，goroutine调度器

# goroutine调度模型
## G-P-M模型
G（goroutine）：每个goroutine在运行时的一个抽象结构，在G的眼中只有P
P（Processor）:逻辑处理器,本地运行队列，用于存放可运行的G
M（machine）：操作系统线程
只有将P和M绑定，才能让P本地运行队列中的G真正运行起来
调度原理：从P的运行队列中取出一个G运行，如果运行队列为空，则尝试从全局队列中或其他P的本地运行队列中取出一个G运行

## 长时间运行的G被抢占调度
问题：不支持抢占式调度（如果P中的G一直占用，那么位于P中的其他G无法被调度，出现“饿死”情况）
在go程序启动时，启动一个名为sysmon的M（一般称为监控线程），无需绑定任务的P即可运行（以g0的这个G形式），将向长时间运行的G任务发出抢占调度
将G抢占并移除运行状态，放入P的本地运行队列（如果已满，则放入全局运行队列中），等待下一次被调度

## G被阻塞在某个channel或网络I/O操作
将该G放到某个等待队列中，而M则会尝试运行P的下一个可运行的G
当I/O操作或channel操作完成，在等待队列中的G会被唤醒，并标记为可运行，放入到p的某个队列中

## G被阻塞在某个系统调用上
G会被阻塞，与此同时执行该G的M也会解绑P,与G一起进入阻塞状态
此时如果有空闲的M,则P会与其他绑定并执行其他G；否则会创建一个新的M
当系统调用返回后，阻塞在系统调用上的G会尝试获取一个可用的P,如果有之前运行G的M则与之绑定继续运行P;
如果没有，G和M关联将解绑，同时G被标记为可运行状态，放到全局运行队列中，等待goroutine调度器再次调度

# 并发模型
并发哲学：不要通过共享内存来通信，而应通过通信来共享内存。

## CSP模型 
一个符合CSP模型的应用程序，应该通过一组输入/输出原语连接起来P的集合

### 并发原语 
1、goroutine:对应CSP模型中的P，封装了数据处理逻辑，是go运行时调度的基本执行单元 
2、channel：对应CSP模型中的输入/输出原语，用于goroutine之间的通信和同步 
3、select：应对多路输入输出，协调多个goroutine进行操作

# 并发模型
## 创建型

## 退出型


